name: friends_badge
description: "Control the Flutter & Friends conference badge."
version: 0.1.0
repository: https://github.com/spydon/friends_badge

environment:
  sdk: ^3.8.1
  flutter: ">=3.32.5"

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.35.5
  nfc_manager: ^4.0.2
  nfc_manager_ndef: ^1.0.1

dev_dependencies:
  flame_lint: ^1.4.1
